# Models

This folder stores trained machine learning models generated during experimentation.

Example:

- intervention_model.pkl

The model is trained using a Random Forest Classifier to predict whether parental intervention is required based on children's app usage behavior.
